import java.util.Scanner;
void main() {
    Scanner scanner = new Scanner(System.in);
    System.out.println("Введи число ");
    int a = scanner.nextInt();
    String s = (a % 2 == 0) ? "Четное" : "Нечетное"; 
    //Если true - Четное, false - наоборот
    System.out.println(s); 
}
